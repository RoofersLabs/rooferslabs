{
  "name": "rooferslabs",
  "version": "0.1.0",
  "private": true,
  "description": "RoofersLabs \u2014 AI-Powered Front Office Platform for Roofing Companies (r1 echo MVP)",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20 <27"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "prepare": "command -v husky >/dev/null 2>&1 && husky || true",
    "setup": "bash scripts/setup-dev.sh",
    "check": "node scripts/check-dev-env.mjs",
    "dev": "bash scripts/dev.sh",
    "dev:api": "npm --prefix apps/api run dev",
    "dev:web": "npm --prefix apps/web run dev",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "format": "prettier --write \"{apps,packages,infra,scripts}/**/*.{ts,tsx,js,jsx,mjs,json,md}\" \"*.{json,md}\" \".*.json\"",
    "format:check": "prettier --check \"{apps,packages,infra,scripts}/**/*.{ts,tsx,js,jsx,mjs,json,md}\" \"*.{json,md}\" \".*.json\"",
    "build": "npm run build --workspace @rooferslabs/shared && npm run build --workspace @rooferslabs/api && npm run build --workspace @rooferslabs/web",
    "build:shared": "npm run build --workspace @rooferslabs/shared",
    "db:up": "docker compose -f docker/docker-compose.yml up -d postgres redis",
    "db:down": "docker compose -f docker/docker-compose.yml down",
    "db:migrate": "npm run prisma:migrate --workspace @rooferslabs/api",
    "db:reset": "npm run prisma:reset --workspace @rooferslabs/api",
    "db:studio": "npm run prisma:studio --workspace @rooferslabs/api",
    "db:seed": "npm run prisma:seed --workspace @rooferslabs/api",
    "prisma:generate": "npm run prisma:generate --workspace @rooferslabs/api"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.8",
    "prettier": "^3.4.2",
    "typescript": "^5.7.3"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx,json,md}": "prettier --write"
  }
}
